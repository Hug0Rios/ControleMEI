import { Card } from '../../../components/ui/Card';

export function Usuario() {
  return (
    <Card>
      <h2 className="text-lg font-semibold">Página de Usuário</h2>
      <p className="mt-2 text-sm text-slate-600">Preferências e dados do usuário.</p>
    </Card>
  );
}
